import { useQuery } from '@tanstack/react-query'; import { getHealth } from '../utils/api'; export default function HealthPage(){ const {data,isLoading,error}=useQuery({queryKey:['health'],queryFn:getHealth}); return (<div className='space-y-4'><h1 className='text-xl font-semibold'>Health</h1><div className='card'><div className='card-body'>{isLoading?'Loading...':error?String((error as any)?.message??error):<pre className='text-sm overflow-auto'>{JSON.stringify(data,null,2)}</pre>}</div></div></div>); }